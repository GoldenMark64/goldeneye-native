diff --git a/src/game/chrprop.c b/src/game/chrprop.c
--- a/src/game/chrprop.c
+++ b/src/game/chrprop.c
@@ -313,8 +313,13 @@ PropRecord* chrpropAllocate(void)
         prop->stan = NULL;
         prop->timetoregen = 0;
         prop->rooms[0] = 0xFF;
+#ifdef GE_PORT_NATIVE
+        gePortPropAllocatorAllocated((unsigned int)(prop - g_Props));
+#endif
         return prop;
     }
-
+#ifdef GE_PORT_NATIVE
+    gePortPropAllocatorAllocationFailed();
+#endif
     return NULL;
 }
@@ -324,5 +329,8 @@ PropRecord* chrpropAllocate(void)
 void chrpropFree(PropRecord *prop)
 {
+#ifdef GE_PORT_NATIVE
+    gePortPropAllocatorFreed((unsigned int)(prop - g_Props));
+#endif
     prop->prev = g_FreeProps;
     prop->next = 0x0;
     prop->stan = 0x0;
diff --git a/src/game/lv.c b/src/game/lv.c
--- a/src/game/lv.c
+++ b/src/game/lv.c
@@ -548,6 +548,9 @@ void lvlStageLoad(s32 stage)
     gePortBootMark("lv:initModelHitEntryFreeList"); initModelHitEntryFreeList();
     gePortBootMark("lv:modelmgrResetSlotCounts"); modelmgrResetSlotCounts();
     gePortBootMark("lv:init_load_objpos_table"); init_load_objpos_table();
+#ifdef GE_PORT_NATIVE
+    gePortPropAllocatorReset(MAX_PROPS, ONSCREEN_PROP_LIST_LEN);
+#endif
     gePortBootMark("lv:reinit_between_menus"); reinit_between_menus();
     gePortBootMark("lv:init_sound_effects_registers"); init_sound_effects_registers();
     gePortBootMark("lv:init_guards"); init_guards();
@@ -593,5 +596,23 @@ void lvlStageLoad(s32 stage)
     gePortBootMark("lv:zbufDeallocate"); zbufDeallocate();
     gePortBootMark("lv:viSetVideoMode"); viSetVideoMode(MD_NORMAL);
     D_80048368 = 1.0f;
     gePortBootMark("lv:lvlSetControlsLockedFlag"); lvlSetControlsLockedFlag(0);
+#ifdef GE_PORT_NATIVE
+    {
+        const char *build_variant =
+#if defined(VERSION_JP)
+            "jp";
+#elif defined(VERSION_EU)
+            "eu";
+#elif defined(VERSION_US)
+            "us";
+#else
+            "unknown";
+#endif
+        gePortPropAllocatorStageReady(
+            build_variant, (int)stage, (int)lvlGetSelectedDifficulty(),
+            (int)getPlayerCount(), (unsigned long long)(unsigned int)g_GlobalTimer,
+            (unsigned long long)gePortRenderedFrame());
+    }
+#endif
 }
diff --git a/src/ge_port_decls.h b/src/ge_port_decls.h
--- a/src/ge_port_decls.h
+++ b/src/ge_port_decls.h
@@ -2,6 +2,14 @@ extern void gePortBootMark(const char *what);   /* port boot tracing; see port_su
 extern void gePortCheckItemEntries(const char *where); /* c_item_entries corruption canary */
 extern int gePortNetInit(void);   /* netplay boot: opens a socket if GETV_NET_HOST/JOIN is set; see ge_net_udp.c */
 extern int gePortNetTick(void);   /* netplay per-tick: 1 to proceed, 0 to stall; see ge_net_udp.c */
+extern unsigned long gePortRenderedFrame(void);
+extern void gePortPropAllocatorReset(unsigned int capacity, unsigned int onscreen_capacity);
+extern void gePortPropAllocatorAllocated(unsigned int slot_index);
+extern void gePortPropAllocatorAllocationFailed(void);
+extern void gePortPropAllocatorFreed(unsigned int slot_index);
+extern void gePortPropAllocatorStageReady(const char *build_variant, int stage_id, int difficulty,
+                                          int player_count, unsigned long long game_tick,
+                                          unsigned long long render_frame);
 /* GENERATED by tools/gen_port_decls.py - do not edit by hand.
  *
  * IDO accepted implicit function declarations; C99+/clang does not. Every
